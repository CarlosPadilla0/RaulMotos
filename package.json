{
  "name": "coppel-motorcycle-checkout",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Aplicación de checkout para motocicletas de Coppel",
  "author": "Coppel",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "node start-server.js",
    "start:simple": "npx serve -s build -p 10000",
    "serve": "npx serve -s build",
    "lint": "echo 'No linting configured'",
    "type-check": "tsc --noEmit",
    "prebuild": "echo 'Starting build process...'",
    "postbuild": "echo 'Build completed successfully!'",
    "verify-render": "node verify-render.js",
    "deploy-check": "npm run verify-render && npm run type-check && npm run build"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "serve": "^14.2.5",
    "tailwindcss": "^3.4.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
